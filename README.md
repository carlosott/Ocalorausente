
  # Responsive landing page design

  This is a code bundle for Responsive landing page design. The original project is available at https://www.figma.com/design/CPhi0em3VHAamR8QU8YC9R/Responsive-landing-page-design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  